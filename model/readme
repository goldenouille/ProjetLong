@Alice

dans methode et attribut besoin d'un pointeur vers l'id de la classe maitresse (attribut nom : class) 
    -> j'ai rajout une pointeur vers la classe (de type vertex), tu veux que ce soit un int? sinon je crains que "class" ne soit un mot-cl qu'on ne puisse pas donn comme nom de variable.

implementer edges en tout genre
completer part -> fait en partie
hashmap pour lier id avec objet accessible partout
